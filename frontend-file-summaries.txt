{
  "frontend/src/index.css": "Global styles with the Silk Splendor brand design system — OKLCH color tokens for crimson, gold, and ivory, Google Fonts imports, and component classes (saree-card, page-header, admin-table-row) rewritten to use plain CSS instead of @apply with opacity modifiers to avoid Tailwind build errors.",
  "frontend/tailwind.config.js": "Extended Tailwind config with brand font families (heading, display, body), custom color tokens (crimson, gold, ivory, teal), silk-themed box shadows, and shimmer animation.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — `useAddSaree` and `useUpdateSaree` mutations now wrap backend calls with `retryWithBackoff` (3 attempts, exponential backoff), while all other hooks remain unchanged.",
  "frontend/src/App.tsx": "Main app with TanStack Router — Catalog is the default '/' route; all three admin routes (/admin/sarees, /admin/orders, /admin/customers) are wrapped with OwnerVerificationGate to block unauthorized access.",
  "frontend/src/components/Layout.tsx": "Persistent app shell with sticky header (brand logo showing only \"Pure Silk. Timeless Elegance.\" tagline, conditional admin nav, mobile menu), main content area, and footer — footer brand section now shows \"Tradition Woven in Every Thread\" without the \"Fine Silk Sarees\" prefix.",
  "frontend/src/pages/Catalog.tsx": "Public saree catalog page with hero banner, search/filter, product grid — each saree card now has an \"Order Now\" button (disabled and labeled \"Out of Stock\" when stock is 0) that opens the OrderPlacementModal for the selected saree.",
  "frontend/src/pages/AdminSarees.tsx": "Admin saree management page with add/edit modal featuring automatic image compression, upload progress bar, descriptive error messages, and retry-aware loading states — `CompressedImageInfo.bytes` is now typed as `Uint8Array<ArrayBuffer>` and all fallback `arrayBuffer()` calls use explicit `as ArrayBuffer` casts to fix the TypeScript type error.",
  "frontend/src/pages/AdminOrders.tsx": "Admin orders page with extended table showing all columns (Order ID, Customer, Phone, Product Name, Fabric, Color, Qty, Unit Price, Total, Payment Status, Date & Time, Status), color-coded status badges for all five statuses including Cancelled, inline status dropdown, and auto-refresh via the 15-second refetchInterval in useGetAllOrders.",
  "frontend/src/pages/AdminCustomers.tsx": "Admin customers page with stats, a searchable table showing name, phone, email, address, and order count, plus an add/edit modal form with validation for all customer fields.",
  "frontend/index.html": "HTML entry point with the browser tab title updated to \"KBR Silks\".",
  "frontend/src/config/constants.ts": "Centralized business config exporting phone numbers, display variants, WhatsApp link, and an `OWNER_PHONE_NUMBERS` array now containing three authorized owner numbers: `9573147399`, `7981314611`, and `7981324611`.",
  "frontend/src/hooks/useOwnerAuth.ts": "Custom React hook that manages owner phone verification state using sessionStorage — provides `isVerified`, `verifyPhoneNumber` (checks against OWNER_PHONE_NUMBERS), and `clearVerification`.",
  "frontend/src/components/OwnerVerificationGate.tsx": "Auth gate component that wraps admin pages — shows a styled phone number entry form if the user is not verified, displays an \"Access denied\" error for unauthorized numbers, and renders children once verified.",
  "frontend/src/components/OrderPlacementModal.tsx": "Modal component for placing orders from the catalog — collects customer name, phone, and quantity, shows a product preview, calculates total price, calls usePlaceOrder mutation, invalidates the `orders` React Query cache on success so AdminOrders refreshes immediately, and displays a success confirmation screen with order ID.",
  "frontend/src/utils/imageCompression.ts": "Client-side image compression utility using the Canvas API — all `Uint8Array` types are now explicitly typed as `Uint8Array<ArrayBuffer>` with `as ArrayBuffer` casts on `arrayBuffer()` calls to satisfy the strict TypeScript generic constraint required by `ExternalBlob.fromBytes`.",
  "frontend/src/utils/retryWithBackoff.ts": "New utility module providing a generic `retryWithBackoff` async wrapper with exponential backoff (1s, 2s, 4s delays, up to 3 attempts) and a `parseBackendError` helper that converts raw backend errors into user-friendly messages distinguishing image-too-large, unauthorized, timeout, and network failures."
}